Create a .env file in the parent directory like this 
DB_USER="..." 
DB_PASSWORD="..."  

In terminal: 
cd backend 
python -m venv venv 
pip install requirements.txt 
uvicorn main:app --reload   

Open the index.html file in Browser and start Command Gatewaying
